{
  "name": "cap2backend",
  "version": "1.0.0",
  "description": "We have two databases, fav.json and current.json, and each one has its own properties.\r fav.json :\r Suggestions are saved in the user's favourites, and he can return to them whenever he wants\r Or\r current.json :\r You save the details of the current item in the site and save only one item\r allController.js\r It performs all categories and displays them simultaneously. As for the Controller, it is a function that fetches, saves, edits, and also deletes what is required of the user.\r ebookController.js\r It displays books and either Controller is a function that fetches, saves, edits, and also deletes what is required of the user to do.\r softwareController.js\r It is displaying systems, updates, and other programming matters. As for Controller, it is a function that fetches, saves, or modifies, and also deletes what is required of the user.\r searchController.js\r It is a quick feature that searches for what the user wants and what is required of the Wama Controller. It is a function that fetches, saves, or edits and also deletes what is required to be done by the user.\r podcastController.js\r They are conversations or a group of people talking to each other, and the Controller is a function that fetches, saves, edits, and also deletes what is required to be done by the user.\r musicVideoController.js\r It is music with the video, and the Controller is a function that fetches, saves, edits, and also deletes what the user is required to do.\r musicController.js\r It displays songs only without video, and the Controller is a function that fetches, saves, edits, and also deletes what is required of the user.\r moviesController.js\r Show movies and their seasons, and either Controller is a function that fetches, saves, edits, and also deletes what is required of the user to do.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && node index.js",
    "dev": "nodemon index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/group2-qassim/cap2backend.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/group2-qassim/cap2backend/issues"
  },
  "homepage": "https://github.com/group2-qassim/cap2backend#readme",
  "dependencies": {
    "axios": "^0.24.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "helmet": "^4.6.0",
    "morgan": "^1.10.0"
  }
}
